<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/style.css">
    <title><%= post ? 'Edit Post' : 'Create Post' %></title>
</head>
<body>
    <h1><%= post ? 'Edit Post' : 'Create Post' %></h1>
    
    <form action="<%= post ? '/post/' + post._id + '/edit' : '/edit' %>" method="POST">
        <label for="title">Title</label>
        <input type="text" id="title" name="title" value="<%= post ? post.title : '' %>" required>
        
        <label for="content">Content</label>
        <textarea id="content" name="content" rows="4" required><%= post ? post.content : '' %></textarea>
        
        <button type="submit"><%= post ? 'Save Changes' : 'Create Post' %></button>
    </form>

    <a href="/">Back to Home</a>
</body>
</html>
